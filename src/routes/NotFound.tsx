import { Card } from "../components/Card";

function NotFound() {
  return (
    <div className="h-screen flex items-center justify-center">
      <Card title="Not Found"></Card>
    </div>
  );
}

export default NotFound;
